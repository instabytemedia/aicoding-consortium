---
aicc: "1.0"
kind: feature-spec
id: com.example.nested
version: 0.1.0
---

# Nested

## Overview
Basic-class document with nested elaboration and plus-bullets.

## Requirements
+ The API MUST validate input.
+ Errors SHOULD be logged.
  + with a correlation id
  + and a timestamp
